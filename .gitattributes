*.MOV filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.webm filter=lfs diff=lfs merge=lfs -text

# The optimized hero loop is intentionally committed as a regular Git asset so
# Vercel and Netlify receive the playable file without an LFS checkout step.
public/hero/facility-cinematic.mp4 -filter -diff -merge -text
