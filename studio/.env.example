# Copy this file to `.env` and fill in your Sanity project id.
# Create a project at https://www.sanity.io/manage (or run `npx sanity init` here).
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
